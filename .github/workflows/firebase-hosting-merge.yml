# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools
#
# NOTE: Disabled automatic trigger — this duplicated firebase-deploy.yml,
# which now correctly handles auth and also deploys Firestore rules/indexes.
# Keeping this only as a manually-triggerable fallback. Safe to delete this
# file entirely if you don't need it.

name: Deploy to Firebase Hosting on merge
on:
  workflow_dispatch:
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - working-directory: frontend
        run: npm ci && npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_DINECONNECT_36BC7 }}
          channelId: live
          projectId: dineconnect-36bc7
